# action.yml
name: Octahe build
description: Building Octahe
inputs:
  test-build:
    description: Build system for Octahe
    required: true
    default: Octahe
outputs:
  time:
    description: Building Octahe
runs:
  using: docker
  image: .buildcontainer/Dockerfile
  args:
    - ${{ inputs.test-build }}
