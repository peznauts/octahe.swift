# action.yml
name: Octahe build
description: Building Octahe
outputs:
  time:
    description: Running functional Octahe test
runs:
  using: docker
  image: .buildcontainer/Dockerfile
  args:
    - ${{ inputs.test-build }}
